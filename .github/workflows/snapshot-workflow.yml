name: App CI

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  call-build:
    uses: khanmofis/gfs-workflow-files/.github/workflows/maven-build.yml@main
    with:
      java-version: '21'
      maven-goal: 'verify'
      project-key: 'maven-project'
      sonar-token: 'sqp_fec6df03686077fd4ae4596bcad75f982c1c592b'
      sonar-host-url: 'http://localhost:9001'


