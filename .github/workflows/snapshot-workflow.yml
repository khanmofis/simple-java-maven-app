name: App CI

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  call-build:
    uses: khanmofis/gfs-workflow-files/.github/workflows/maven-build.yml@main
    with:
      java-version: '21'
      maven-goal: 'verify'
      project-name: 'maven-project'
      project-key: 'maven-project'
      sonar-token: 'sqp_0bad5f47f094f8956587a490584347ee0938602e'
      sonar-host-url: 'http://localhost:9001'


